{% for table in tables %}
    <h2>{{table['name']}}</h2>
    <div class="table-responsive">
        <table class="table">
        <thead class="thead-dark">
            <tr>
            {% for head in table['columns'] %}
                <th>{{ head }}</th>
            {% endfor %}
            </tr>
        </thead>
            {% for row in table['rows'] %}
                <tr>
                    {% for r in row['rrow'] %}
                    <td>
                        {% if loop.first %}
                            <a href="?command=read&tblid={{table['name']}}&id={{row['pk']}}">{{r}}</a> <a href="?command=update&tblid={{table['name']}}&id={{row['pk']}}">Edit</a>|<a href="?command=delete&tblid={{table['name']}}&id={{row['pk']}}">Delete</a>
                        {% else %}
                            {{r}}
                        {% endif %}
                    </td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </table>
    </div>
    <hr>
{% endfor %}